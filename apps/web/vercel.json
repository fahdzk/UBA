
{
  "buildCommand": "cd ../.. && pnpm --filter @uba/database exec prisma generate && pnpm turbo build --filter=web",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install",
  "regions": ["iad1"],
  "crons": [
    {
      "path": "/api/cron/expire-trials",
      "schedule": "0 0 * * *"
    }
  ]
}
